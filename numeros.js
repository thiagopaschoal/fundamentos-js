console.log(Number.isInteger(2))
console.log(Number.isInteger(2.1))

const peso1 = 3
const peso2 = 2

const nota1 = 7.5
const nota2 = 6.75

const total = (nota1 * peso1) + (nota2 * peso2)
const media = total / (peso1 + peso2)

console.log(media.toFixed(2))
console.log(media.toExponential(2));
console.log(0 / media)
console.log(media / 0)
console.log((13).toFixed(1))
console.log("10" / 2);
console.log("3" + 2);
console.log(media);
